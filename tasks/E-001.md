# E-001

  - role: epic
  - impact: high
  - estimatedEffort: 2w
  - prerequisites: []
  - blocking: [E-002, E-003, E-004, E-005, E-006]
  - filesAffected: [skills/mapctx-tasks/references/field-types-and-contract.md, skills/mapctx-tasks/references/custom-statuses.md, skills/mapctx-tasks/references/status-and-sync.md, rules/AGENTS.md, rules/CLAUDE.md, rules/.cursorrules, TASKS.md]
  - testsRequired: [npm run test:sync-engine, npm test]
  - summary: Define Spec-Driven v3 contract foundations with `specMode` and `ready-for-do` as the default lifecycle model.
  - description: |
      ## Product Context
      MapCtx needs one shared task contract across skills, tooling, and docs so execution quality does not depend on who runs the workflow.

      ## User Story
      As a maintainer, I want a single and explicit contract for task lifecycle and specification depth so every agent run is predictable and auditable.

      ## Acceptance
      - [ ] Canonical contract includes `specMode` and keeps backward compatibility.
      - [ ] Default status lifecycle includes `ready-for-do`.
      - [ ] Rule packs and references align to the same definitions.
      - [ ] Migration guidance is available for current boards.

      ## Steps
      - [ ] lock naming and lifecycle decisions.
      - [ ] update field and status references.
      - [ ] align rule files and skill language.
      - [ ] publish migration notes and rollout order.
