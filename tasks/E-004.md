# E-004

  - role: epic
  - impact: high
  - estimatedEffort: 2w
  - prerequisites: [E-001]
  - blocking: [E-006]
  - filesAffected: [packages/sync-engine/, packages/core/, packages/vscode-extension/, docs/]
  - testsRequired: [npm run test:sync-engine, npm test]
  - summary: Deliver enforcement tooling to validate contract compliance and compute execution order from task dependencies.
  - description: |
      ## Product Context
      Skill guidance is not enough to keep distributed edits consistent. MapCtx needs deterministic checks and execution planning tools.

      ## User Story
      As a maintainer, I want CLI validation and DAG planning so I can trust task quality and run work in the right order.

      ## Acceptance
      - [ ] `mapctx validate` checks schema, IDs, dependencies, and references.
      - [ ] `mapctx plan` computes DAG waves and next runnable tasks.
      - [ ] Output supports both terminal and diagram consumption.
      - [ ] CI enforces validation on relevant changes.

      ## Steps
      - [ ] implement validator MVP and full checks.
      - [ ] implement DAG planner with cycle detection.
      - [ ] add text and Mermaid outputs.
      - [ ] wire checks into CI.
