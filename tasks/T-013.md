# T-013

  - role: feature
  - impact: high
  - estimatedEffort: 1d
  - prerequisites: [T-012]
  - blocking: [T-016, T-017, T-018, T-019, T-027]
  - filesAffected: [skills/mapctx-tasks/references/field-types-and-contract.md, rules/AGENTS.md, TASKS.md]
  - testsRequired: [npm run test:sync-engine]
  - summary: Add `specMode` to the canonical task model and preserve backward compatibility.
  - description: |
      ## Product Context
      Specification depth must be machine-readable to support adaptive workflows without changing core task structure.

      ## User Story
      As a planner, I want each task to declare or inherit a specification depth mode so planning rigor matches risk.

      ## Acceptance
      - [ ] Contract documents `specMode: lite|standard|strict|null`.
      - [ ] Field order guidance includes `specMode` in extension order.
      - [ ] Existing tasks remain valid when `specMode` is absent or null.

      ## Steps
      - [ ] update field contract reference.
      - [ ] update templates with optional `specMode`.
      - [ ] add compatibility note for existing boards.
