# T-024

  - role: feature
  - impact: medium
  - estimatedEffort: 1d
  - prerequisites: [T-023]
  - blocking: [T-026]
  - filesAffected: [skills/mapctx-ralph-tasks/SKILL.md, skills/mapctx-ralph-tasks/references/flow.md]
  - testsRequired: [npm test]
  - summary: Add optional parallel evaluator mode with automatic fallback to isolated sequential mode.
  - description: |
      ## Product Context
      Some runtimes support parallel evaluation while others do not, so execution must adapt without losing quality.

      ## User Story
      As an operator, I want evaluator parallelism when available and a safe fallback when not available.

      ## Acceptance
      - [ ] Mode supports `auto`, `true`, and `false` behavior.
      - [ ] Fallback behavior is explicit in logs and reports.
      - [ ] Quality gates remain identical across modes.

      ## Steps
      - [ ] define capability check and fallback rules.
      - [ ] implement mode selection in workflow.
      - [ ] document operational behavior per mode.
