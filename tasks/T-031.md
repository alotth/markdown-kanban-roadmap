# T-031

  - role: chore
  - impact: medium
  - estimatedEffort: 1d
  - prerequisites: [T-028, T-030]
  - blocking: []
  - filesAffected: [.github/workflows/, package.json, docs/]
  - testsRequired: [npm test, npm run test:sync-engine]
  - summary: Integrate validation and planning checks into CI to enforce contract and ordering quality.
  - description: |
      ## Product Context
      Local-only checks are easy to skip; CI enforcement keeps contract quality stable across contributors.

      ## User Story
      As a repository maintainer, I want CI to fail on invalid task contracts so regressions never merge unnoticed.

      ## Acceptance
      - [ ] CI runs `mapctx validate` on relevant changes.
      - [ ] CI optionally runs planner sanity checks.
      - [ ] Failure messages are clear and actionable.

      ## Steps
      - [ ] add commands to CI pipeline.
      - [ ] scope checks to affected paths where possible.
      - [ ] document local pre-check equivalents.
